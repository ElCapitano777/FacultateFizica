#include <stdio.h>
#include <math.h>
#define n 4


void afisare_vector (double x[n+1])
{
    int i;
    for(i=1;i<n+1;i++)
    {printf("%f, ",x[i]);}
    printf("\n\n");
}

void afisare_matrice(double a[n][n])
{
    int i,j;
    for (i=0;i<=n;i++)
    {
        for (j=0;j<=n;j++)
        {
            if (a[i][j]>-1)
                {printf("+%.0f, ",a[i][j]);}
                else
                {printf("%.0f",a[i][j]);}
        }
        printf("\n");
    }
    printf("\n");
}



void sist_gauss1(double a[n][n],double b[n])
{
    int i,j,m;
    for (i=0;i<n;i++)
    {
        {a[i][j]=a[i][j]/a[i][i];}
        b[i]=b[i]/a[i][i];
        a[i][i]=a[i][i]/a[i][i];
        for(m=i;i<n;m++)
        {
            
            
        }
    }
}

void copiere_vector(double b[n],double e[n])
{
    int i;
    
    for(i=0;i<=n;i++)
    {e[i]=b[i];}
}

void copiere_matrice(double a[n][n],double d[n][n])
{
    int i,j;
    for(i=0;i<=n;i++)
    {   
        for(j=0;j<=n;j++)
        {
            d[i][j]=a[i][j];
        }
    }
}



void sist_gauss2(double a[n][n],double b[n])
{
    int i,j,k;
    double d[n][n],e[n];

//    copiere_matrice(a,d);
//    printare_matrice(a);
//    copiere_vector(b,e);    
    for(k=1;k<=n;k++)
    {
        copiere_matrice(a,d);
        copiere_vector(b,e);
            
        a[k][k]=1;            
        for (j=k;j<=n;j++)
        {
            a[k][j]=d[k][j]/d[k][k];
        }
        b[k]=e[k]/d[k][k];
        for(i=k;i<=n;i++)
        {
            a[i][k]=0;
            for(j=k;j<=n;j++)
            {
                a[i][j]=d[i][j]-(d[i][k]*d[k][j]);
            }
            b[i]=e[i]-(d[i][k]*b[k]);
                afisare_matrice(a);
    afisare_vector(b);
        }
    afisare_matrice(a);
    afisare_vector(b);
    }
    afisare_matrice(a);
    afisare_vector(b);
//    sist_triunghi(a,b,e);
//    afisare_vector(e);

}

void init_A(double A[n][n],double x[n])
{
    int i,j;
    for (i=0;i<=n;i++)
    {
        for (j=0;j<=n;j++)
        {
            A[i][j]=pow(x[i],j);
        }
    }
    
}

void main()
{
    double A[n][n],a[n],y[n]={0,0.4,0.42,0.03},x[n]={0,2,4,6};
    init_A (A,x);
    afisare_matrice (A);

    return 0;
}